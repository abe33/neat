// Generated by CoffeeScript 1.3.3
(function() {
  var Neat, Packager, asyncErrorTrap, ensure, error, find, green, info, neatTask, op, parallel, puts, read, readFileSync, readFiles, red, rm, run, _ref, _ref1, _ref2;

  readFileSync = require('fs').readFileSync;

  Neat = require('../../neat');

  Packager = require('./packager');

  op = require('./operators');

  parallel = Neat.require('async').parallel;

  _ref = Neat.require('utils/commands'), run = _ref.run, neatTask = _ref.neatTask, asyncErrorTrap = _ref.asyncErrorTrap;

  _ref1 = Neat.require('utils/logs'), error = _ref1.error, info = _ref1.info, green = _ref1.green, red = _ref1.red, puts = _ref1.puts;

  _ref2 = Neat.require('utils/files'), ensure = _ref2.ensure, rm = _ref2.rm, find = _ref2.find, readFiles = _ref2.readFiles;

  read = Neat.require('utils/cup').read;

  exports['package'] = neatTask({
    name: 'package',
    description: 'Generates packages for this projects',
    environment: 'default',
    action: function(callback) {
      var conf, dir, tmp, _ref3;
      _ref3 = Neat.config.tasks["package"], dir = _ref3.dir, conf = _ref3.conf, tmp = _ref3.tmp;
      return rm(dir, asyncErrorTrap(function() {
        return ensure(dir, asyncErrorTrap(function() {
          return find('cup', conf, asyncErrorTrap(function(files) {
            return readFiles(files, function(err, res) {
              var c, commands, p;
              commands = (function() {
                var _results;
                _results = [];
                for (p in res) {
                  c = res[p];
                  _results.push(Packager.asCommand(read(c)));
                }
                return _results;
              })();
              return parallel(commands, function() {
                info(green('all package processed'));
                return typeof callback === "function" ? callback() : void 0;
              });
            });
          }));
        }));
      }));
    }
  });

}).call(this);
