// Generated by CoffeeScript 1.3.3
(function() {
  var __indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  Array.isArray = function(a) {
    return (a != null ? a.type() : void 0) === 'array';
  };

  Array.prototype.compact = function() {
    return this.select(function(el) {
      return el != null;
    });
  };

  Array.prototype.empty = function() {
    return this.length === 0;
  };

  Array.prototype.first = function() {
    if (this.length > 0) {
      return this[0];
    } else {
      return void 0;
    }
  };

  Array.prototype.flatten = function(level) {
    var a, el, _i, _len;
    if (level == null) {
      level = Infinity;
    }
    if (level < 0) {
      level = Infinity;
    }
    a = [];
    for (_i = 0, _len = this.length; _i < _len; _i++) {
      el = this[_i];
      if (Array.isArray(el) && level !== 0) {
        a = a.concat(el.flatten(level - 1));
      } else {
        a.push(el);
      }
    }
    return a;
  };

  Array.prototype.group = function(size) {
    var a;
    a = [];
    this.step(size, function() {
      var v;
      return a.push((function() {
        var _i, _len, _results;
        _results = [];
        for (_i = 0, _len = arguments.length; _i < _len; _i++) {
          v = arguments[_i];
          _results.push(v);
        }
        return _results;
      }).apply(this, arguments));
    });
    return a;
  };

  Array.prototype.last = function() {
    if (this.length > 0) {
      return this[this.length - 1];
    } else {
      return void 0;
    }
  };

  Array.prototype.reject = function(f) {
    var o, _i, _len, _results;
    _results = [];
    for (_i = 0, _len = this.length; _i < _len; _i++) {
      o = this[_i];
      if (!(typeof f === "function" ? f(o) : void 0)) {
        _results.push(o);
      }
    }
    return _results;
  };

  Array.prototype.rotate = function(amount) {
    var direction, i, out, _i, _j, _ref, _ref1;
    if (amount == null) {
      amount = 1;
    }
    if (amount === 0) {
      amount = 1;
    }
    direction = amount > 0;
    out = this.concat();
    if (direction) {
      for (i = _i = 0, _ref = Math.abs(amount) - 1; 0 <= _ref ? _i <= _ref : _i >= _ref; i = 0 <= _ref ? ++_i : --_i) {
        out.push(out.shift());
      }
    } else {
      for (i = _j = 0, _ref1 = Math.abs(amount) - 1; 0 <= _ref1 ? _j <= _ref1 : _j >= _ref1; i = 0 <= _ref1 ? ++_j : --_j) {
        out.unshift(out.pop());
      }
    }
    return out;
  };

  Array.prototype.select = function(f) {
    var o, _i, _len, _results;
    _results = [];
    for (_i = 0, _len = this.length; _i < _len; _i++) {
      o = this[_i];
      if (typeof f === "function" ? f(o) : void 0) {
        _results.push(o);
      }
    }
    return _results;
  };

  Array.prototype.step = function(n, f) {
    var i, _i, _ref, _results;
    _results = [];
    for (i = _i = 0, _ref = Math.ceil(this.length / n) - 1; 0 <= _ref ? _i <= _ref : _i >= _ref; i = 0 <= _ref ? ++_i : --_i) {
      _results.push(f != null ? f.apply(this, this.slice(i * n, (i * n + n - 1) + 1 || 9e9)) : void 0);
    }
    return _results;
  };

  Array.prototype.uniq = function() {
    var out, v, _i, _len;
    out = [];
    for (_i = 0, _len = this.length; _i < _len; _i++) {
      v = this[_i];
      if (__indexOf.call(out, v) < 0) {
        out.push(v);
      }
    }
    return out;
  };

  Array.prototype.min = function() {
    return Math.min.apply(null, this);
  };

  Array.prototype.max = function() {
    return Math.max.apply(null, this);
  };

}).call(this);
