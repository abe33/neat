// Generated by CoffeeScript 1.3.3
(function() {
  var def,
    __indexOf = [].indexOf || function(item) { for (var i = 0, l = this.length; i < l; i++) { if (i in this && this[i] === item) return i; } return -1; };

  def = function(o) {
    var name, value, _results;
    _results = [];
    for (name in o) {
      value = o[name];
      if (Object.prototype[name] == null) {
        _results.push(typeof Object.defineProperty === "function" ? Object.defineProperty(Object.prototype, name, {
          enumerable: false,
          value: value
        }) : void 0);
      } else {
        _results.push(void 0);
      }
    }
    return _results;
  };

  Object["new"] = function(a) {
    var o;
    o = {};
    a.step(2, function(k, v) {
      return o[k] = v;
    });
    return o;
  };

  def({
    contains: function(value) {
      return this.has(value);
    }
  });

  def({
    flatten: function() {
      var a, k, v;
      a = [];
      for (k in this) {
        v = this[k];
        a = a.concat([k, v]);
      }
      return a;
    }
  });

  def({
    has: function(value) {
      return __indexOf.call(this.values(), value) >= 0;
    }
  });

  def({
    hasKey: function(key) {
      return this[key] != null;
    }
  });

  def({
    keys: function() {
      var k, _results;
      _results = [];
      for (k in this) {
        _results.push(k);
      }
      return _results;
    }
  });

  def({
    length: function() {
      return this.keys().length;
    }
  });

  def({
    merge: function(o) {
      var k, v, _results;
      _results = [];
      for (k in o) {
        v = o[k];
        _results.push(this[k] = v);
      }
      return _results;
    }
  });

  def({
    reject: function(f) {
      var k, o, v;
      o = {};
      for (k in this) {
        v = this[k];
        if (!(typeof f === "function" ? f(k, v) : void 0)) {
          o[k] = v;
        }
      }
      return o;
    }
  });

  def({
    select: function(f) {
      var k, o, v;
      o = {};
      for (k in this) {
        v = this[k];
        if (typeof f === "function" ? f(k, v) : void 0) {
          o[k] = v;
        }
      }
      return o;
    }
  });

  def({
    size: function() {
      return this.length();
    }
  });

  def({
    sortedKeys: function() {
      return this.keys().sort();
    }
  });

  def({
    sortedValues: function() {
      var k, _i, _len, _ref, _results;
      _ref = this.sortedKeys();
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        k = _ref[_i];
        _results.push(this[k]);
      }
      return _results;
    }
  });

  def({
    type: function() {
      return Object.prototype.toString.call(this).toLowerCase().replace(/\[object (\w+)\]/, "$1");
    }
  });

  def({
    update: function() {
      return function(o) {
        return this.merge(o);
      };
    }
  });

  def({
    values: function() {
      var k, _i, _len, _ref, _results;
      _ref = this.keys();
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        k = _ref[_i];
        _results.push(this[k]);
      }
      return _results;
    }
  });

  def({
    quacksLike: function(type) {
      var definition, k, v, _ref;
      if (type.__definition__ != null) {
        definition = type.__definition__;
        if (typeof definition === "function") {
          return definition(this);
        }
        for (k in definition) {
          v = definition[k];
          switch (typeof v) {
            case "function":
              if (!v(this[k])) {
                return false;
              }
              break;
            default:
              if (!(v === "*" || ((_ref = this[k]) != null ? _ref.type() : void 0) === v)) {
                return false;
              }
          }
        }
        return true;
      } else {
        return false;
      }
    }
  });

}).call(this);
