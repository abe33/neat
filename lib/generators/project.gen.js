// Generated by CoffeeScript 1.3.3
(function() {
  var Neat, basename, describe, ensureSync, error, exists, extname, hashArguments, missing, namespace, neatBroken, project, puts, render, resolve, touchSync, usages, warn, _ref, _ref1, _ref2, _ref3,
    __slice = [].slice;

  _ref = require('path'), resolve = _ref.resolve, exists = _ref.existsSync, basename = _ref.basename, extname = _ref.extname;

  Neat = require('../neat');

  _ref1 = Neat.require("utils/logs"), puts = _ref1.puts, error = _ref1.error, warn = _ref1.warn, missing = _ref1.missing, neatBroken = _ref1.neatBroken;

  _ref2 = Neat.require("utils/files"), ensureSync = _ref2.ensureSync, touchSync = _ref2.touchSync;

  namespace = Neat.require("utils/exports").namespace;

  render = Neat.require("utils/templates").renderSync;

  _ref3 = Neat.require("utils/commands"), usages = _ref3.usages, describe = _ref3.describe, hashArguments = _ref3.hashArguments;

  usages('neat generate project <name> {description, author, keywords}', describe('Creates a <name> directory with the default neat project content\nDescription, author and keywords can be defined using the hash\narguments.', project = function() {
    var a, args, b, base, c, callback, context, d, dirs, e, ext, files, generator, name, path, t, tplpath, _i, _j, _k, _len, _len1, _ref4;
    generator = arguments[0], name = arguments[1], args = 4 <= arguments.length ? __slice.call(arguments, 2, _i = arguments.length - 1) : (_i = 2, []), callback = arguments[_i++];
    if (name == null) {
      return error("Missing name argument");
    }
    if (args.length === 0 && typeof callback !== 'function') {
      args.push(callback);
    }
    path = resolve('.', name);
    base = basename(__filename);
    ext = extname(__filename);
    tplpath = resolve(__dirname, "project");
    context = args.empty() ? {} : hashArguments(args);
    context.merge({
      name: name,
      version: Neat.meta.version
    });
    ensureSync(path);
    dirs = ["lib", "src", "src/commands", "src/config", "src/config/environments", "src/config/initializers", "src/generators", "src/tasks", "templates", "test", "test/fixtures", "test/functionals", "test/integrations", "test/units"];
    files = [["lib/.gitkeep"], ["src/commands/.gitkeep"], ["src/config/environments/default.coffee", true], ["src/config/environments/development.coffee", true], ["src/config/environments/production.coffee", true], ["src/config/environments/test.coffee", true], ["src/config/initializers/docco.coffee", true], ["src/generators/.gitkeep"], ["src/tasks/.gitkeep"], ["templates/.gitkeep"], ["test/fixtures/.gitkeep"], ["test/functionals/.gitkeep"], ["test/integrations/.gitkeep"], ["test/test_helper.coffee", true], ["test/units/.gitkeep"], ['.gitignore', true], ['.neat', true], ['.npmignore', true], ['Cakefile', true], ['Nemfile', true]];
    t = function(a, b, c) {
      var _ref4;
      if (c == null) {
        c = false;
      }
      if (typeof b === 'boolean') {
        _ref4 = [a, b], b = _ref4[0], c = _ref4[1];
      }
      if (b == null) {
        b = a;
      }
      if (c) {
        return touchSync(resolve(path, a), render(resolve(tplpath, b), context));
      } else {
        return touchSync(resolve(path, a));
      }
    };
    e = function(d) {
      return ensureSync(resolve(path, d));
    };
    try {
      for (_j = 0, _len = dirs.length; _j < _len; _j++) {
        d = dirs[_j];
        e(d);
      }
      for (_k = 0, _len1 = files.length; _k < _len1; _k++) {
        _ref4 = files[_k], a = _ref4[0], b = _ref4[1], c = _ref4[2];
        t(a, b, c);
      }
    } catch (e) {
      return error("Cannot proceed to the generation of the project\n\n" + e.stack);
    }
    return typeof callback === "function" ? callback() : void 0;
  }));

  module.exports = {
    project: project
  };

}).call(this);
